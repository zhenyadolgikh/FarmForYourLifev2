using System.Collections;
using System.Collections.Generic;
using UnityEngine;


[CreateAssetMenu(menuName = "Special cards/Money printer")]

public class MoneyPrinter : SpecialCard
{
    // Start is called before the first frame update
    
    public MoneyPrinter(string CardName, string CardDescription) : base(CardName, CardDescription)
    {
        
    }
}
