using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Worker
{
    // Start is called before the first frame update
    public int workedId = -1;

    public int workrate = 80;

    public WorkType workType = WorkType.unassigned;
}
